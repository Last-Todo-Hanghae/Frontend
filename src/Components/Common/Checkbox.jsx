import React from "react";
import * as style from "../../Styles/styles";
import { FilledCheck, EmptyCheck } from "../../Assets/icons";

// function Checkbox({ handler, isDone }) {
//   return (
//     <button onClick={handler} style={{ border: "0px" }}>
//       {isDone ? <FilledCheck /> : <EmptyCheck />}
//     </button>
//   );

  // if (isDone) return <style.Checkbox type="checkbox" checked/>
  // else return <style.Checkbox type="checkbox" />
// }

// export default Checkbox;
